# Phase 10 chat edit — purchase controls row

The final Edition purchase card now keeps Unit price, Quantity, Subtotal, and Add to cart on one horizontal row at widths >= 768 px. This removes the large unused gap in the previous two-column layout.

Below 768 px the controls use a compact two-column/two-row layout so touch targets remain usable without horizontal overflow.
